---
title: Left outer join
summary: "Discover left outer joins in SQL, which return all rows from the left table and matching rows from the right table."
headline: Left outer join
def: A join that keeps every row from the first (left-hand) table regardless of whether that row matched any in the second (right-hand) table.
redirect_from:
  - /glossary/left_outer_join
---

## What is a left outer join?

A left outer join is a join that keeps every row from the first (left-hand) table regardless of whether that row matched any in the second (right-hand) table. Columns from the second table are filled with `NULL` if there was no match.
